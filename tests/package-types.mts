import {
  createRender,
  useStoreHTMLElement,
  useStoreInput,
} from "react-store-input";
import { TextEditor } from "react-store-input/text-editor";
import { shallowEqual, useStore, type Store } from "gw-store";

type State = { value: string };

const storeType: Store<State> | undefined = undefined;

void storeType;
void createRender;
void useStoreHTMLElement;
void useStoreInput;
void shallowEqual;
void useStore;
void TextEditor;
