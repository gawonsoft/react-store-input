# react-store-input example

This Vite app exercises the core package without the optional text editor.

```sh
npm install
npm run dev
```

The production build is also part of the root package's `npm test` release
check, which prevents examples and public types from drifting apart.
